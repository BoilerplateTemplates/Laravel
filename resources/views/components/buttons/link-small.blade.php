<a href="{{ $action }}"
	{{ $attributes->class(['inline-flex items-center justify-center rounded-md border border-transparent bg-brand-secondary px-4 py-1 text-xs tracking-tight font-semibold text-brand-primary hover:bg-brand-highlight hover:text-brand-primary sm:w-auto']) }}>
	{{ __($text) }}
</a>
